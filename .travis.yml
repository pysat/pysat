language: python
python:
  - "2.7"
  - "3.6"
sudo: false

cache: pip
cache:		
   directories:		
      - ../pyglow_cache
 
addons:
  apt:
    packages:
    - gfortran
    - libncurses5-dev

# Setup anaconda
before_install:
  #- apt-get update
  - if [[ "$TRAVIS_PYTHON_VERSION" == "2.7" ]]; then
      wget https://repo.continuum.io/miniconda/Miniconda-latest-Linux-x86_64.sh -O miniconda.sh;
    else
      wget https://repo.continuum.io/miniconda/Miniconda3-latest-Linux-x86_64.sh -O miniconda.sh;
    fi
  - bash miniconda.sh -b -p $HOME/miniconda
  - export PATH="$HOME/miniconda/bin:$PATH"
  - hash -r
  - conda config --set always_yes yes --set changeps1 no
  - conda update -q --yes conda
  # Useful for debugging any issues with conda
  - conda info -a
  # Replace dep1 dep2 ... with your dependencies
  - conda create -q -n test-environment python=$TRAVIS_PYTHON_VERSION atlas numpy scipy matplotlib nose pandas statsmodels coverage netCDF4 ipython xarray

# command to install dependencies
install:
  # Coverage packages are on my binstar channel
  # - conda install --yes -c dan_blanchard python-coveralls nose-cov
  - source activate test-environment
  - pip install coveralls
  - pip install future
  - pip install pysatCDF >/dev/null
    # install pyglow, space science models
  - if [[ "$TRAVIS_PYTHON_VERSION" == "2.7" ]]; then
      travis_wait 30 pip install ipdb --user;
      cd ..;
    fi
  - pwd
  - ls
    
   # check if there is a partial download of pyglow, remove if true
  #- export PYGLOW_DIR=./pyglow_cache
  # - if [[ -d "$PYGLOW_DIR" && "$TRAVIS_PYTHON_VERSION" == "2.7" ]]; then
  #    export PYGLOW_SIZE=($(du -s $PYGLOW_DIR));
  #    echo 'PYGLOW_SIZE' $PYGLOW_SIZE;
  #    export CHECK_SIZE=50000;
  #    if [[ "${PYGLOW_SIZE}" -lt "$CHECK_SIZE" ]]; then
  #      rm -rf $PYGLOW_DIR;
  #      echo 'trying to remove pyglow directory';
  #    fi
  #  fi
  #  # If pyglow exists, install it
  #- if [[ -d "$PYGLOW_DIR/pyglow" && -e "$PYGLOW_DIR/pyglow/setup.py" && "$TRAVIS_PYTHON_VERSION" == "2.7" ]]; then
  #    cd $PYGLOW_DIR/pyglow;
  #    python setup.py install;
  #    cd ../..;
  #  fi
    # If no pyglow, clone, then run script
  #- if [[ ! -d "$PYGLOW_DIR/pyglow" || ! -e "$PYGLOW_DIR/pyglow/setup.py" && "$TRAVIS_PYTHON_VERSION" == "2.7" ]]; then
  #    if [[ ! -d "$PYGLOW_DIR" ]]; then 
  #      ls;
  #      echo 'making root pyglow directory';
  #      mkdir $PYGLOW_DIR;
  #    fi;
  #    cd $PYGLOW_DIR;
  #    echo 'dir listing';
  #    ls;
  #    travis_wait 50 git clone https://github.com/timduly4/pyglow.git;
  #    cd ./pyglow;
  #    travis_wait 50 ./pyglow_install.sh >/dev/null;
  #    cd ../..;
  #  fi
  - echo 'cloning pyglow';
  - travis_wait 50 git clone https://github.com/timduly4/pyglow.git;
  - echo 'installing pyglow';
  - cd ./pyglow;
  - travis_wait 50 make -C src/pyglow/models source >/dev/null;
  - travis_wait 50 python setup.py install --user
  - cd ../pysat;
  # install pysat
  - "python setup.py install"
# command to run tests
script: 
  - nosetests -vs --with-coverage --cover-package=pysat
after_success:
  coveralls
