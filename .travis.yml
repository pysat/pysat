language: python
python:
  - "2.7"
notifications:
  email: false
before_install:
  - sudo apt-get install libhdf5-serial-dev netcdf-bin libnetcdf-dev
  - pip install numpy cython python-dateutil netCDF4
install:
  - python setup.py install
script:
  - cd test
